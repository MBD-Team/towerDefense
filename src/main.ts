import './style.css';
type GameMap = {
  enemyPath: boolean;
  isPlayerTower: boolean;
  playerBase: boolean;
  enemyStart: boolean;
  isEnemy: boolean;
}[][];

const gameMap: GameMap = [];

const gameSize = 3;

function renderMap() {
  const;
}
